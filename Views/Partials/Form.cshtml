<div style="text-align: center">
@using(Html.BeginUmbracoForm("SaveForm", "FormSurface"))
{
    @Html.EditorFor(x => Model)
    <input type="submit"/>
}

@if(ViewBag.Result != null ){
    
    <div style="text-align: center">
        @ViewBag.Result
    </div>
}
</div>